#include "main.h"
/**
 * main - Entry point
 * Return: always (0)
 */

void print_alphabet(void)
{
	char c;
	int i = 0;

	while (i <= 0)
	{
		for (c= 'a'; c <= 'z'; c++)
		{
			_putchar(c);
		}
			_putchar('\n');
			i++;
	}
}

