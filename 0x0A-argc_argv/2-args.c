#include <stdio.h>

/**
 *main - where program bigins
 *@argc: command line count
 *@argv: command line vector
 *Return: always 0
 */

int main(int argc, char *argv[])
{

	int i;

	if (i = 0; i < argc; i++)

	{
		printf("%s\n", argv[i]);
	}

	return (0);
}

